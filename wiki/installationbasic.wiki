#labels Featured
=Basic Installation=

  # Install the !FileBrowser anywhere on your python-path. I´m personally using the project-directory.
{{{
svn checkout http://django-filebrowser.googlecode.com/svn/trunk/ filebrowser
}}}
  # Copy the folder /media either to your admin media-directory (cp -R /path/to/filebrowser/media/filebrowser /path/to/your/admin/media/) or to your preferred media-directory (in this case, you have to edit PATH_FILEBROWSER_MEDIA in fb_settings).
  # Copy /templates to your admin template-directory (cp -R /path/to/filebrowser/templates/filebrowser /path/to/your/admin/templates/)
  # Change fb_settings.py. Or better, overwrite the filebrowser settings in your project settings-file.
  # Change your urls.py, add the following lines BEFORE the admin-urls:
{{{
(r'^admin/filebrowser/', include('filebrowser.urls')),
}}}
  # Add the !FileBrowser to your Admin Indexpage (/templates/admin/index.html). Add this code BEFORE {% for app in app_list %} ... {% endfor %}:
{{{
<div class="module">
    <table>
        <caption>File-Browser</caption>
        <tr class="row2">
            <th scope="row"><a href="filebrowser/">File-Browser</a></th>
            <td>&nbsp;</td>
            <td>&nbsp;</td>
        </tr>
    </table>
</div>
}}}
  # That's it.